from telegram import Update
from telegram.ext import ApplicationBuilder, CommandHandler, MessageHandler, filters, ContextTypes

questions = [
    {
        "question": "Какой язык программирования используется для этого бота?",
        "options": ["Python", "Java", "C++", "JavaScript"],
        "answer": "Python"
    },
    { 
        
    },
    {
        
    },
   "question":  "Столица  Франции?", 
        "options":  ["Берлин",  "Париж",  "Лондон",  "Мадрид"], 
        "ответ":  "Париж" 
    } 
] 


    }
]

#  Индекс  текущего  вопроса current_question_index = 0 # Стартовая команда async def start(update: Update, context: ContextTypes.DEFAULT_TYPE): global current_question_index 
current_question_index  =  0 

await send_question(update, context) # Отправка вопросов async def send_question(update: Update, context: ContextTypes.DEFAULT_TYPE): вопрос = вопросы[текущий_индекс_вопроса] options = вопрос["опции"] вопрос_текст = вопрос["вопрос"] + "\n" для idx, опция в перечислении(опции, 1): вопрос_текст += f"{idx}. {option}\n" ждут обновления

    
     .message.reply_text(question_text)

# Обработка ответа пользователя
async def handle_answer(
    current_question_index = update: Update, context: ContextTypes.DEFAULT_TYPE):
    global current_question_index
    user_answer = update.message.text.strip()

    question = questions[current_question_index]
    correct_answer = question[
    user_answer = update.message.text.strip()

    question = questions[current_question_index]
    correct_answer


    question = questions[current_question_index]
    options = question[
    question_text = question[
        question_text += "answer"]
    options = question["options"]
    correct_option_number = options.index(correct_answer) + 1

    if user_answer == str(correct_option_number):
        await update.message.reply_text("Правильно! Следующий вопрос...")
        current_question_index += 1
        if current_question_index < len(questions):
            await send_question(update, context)
        else:
            await update.message.reply_text("Поздравляем! Вы прошли игру.")
    else:
        await update.message.reply_text(f"Неправильно. Правильный ответ: {correct_option_number}. {correct_answer}")

def main():
    # Вставьте сюда ваш токен
    TOKEN = 'ВАШ_ТОКЕН_ТУТ'
    app = ApplicationBuilder().token(TOKEN).build()

    app.add_handler(CommandHandler("start", start))
    app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_answer))

    app.run_polling()

if __name__ == '__main__':
    main()
